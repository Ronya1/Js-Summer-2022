/* class ResUserClass {
/**
 * 1.) create a function to check if the the Res is already registered
 *      function isResExisting (restaurantName, resAddress) {
 *          if ( restaurantName && resAddress !== 0) 
 *          Console.log('Account Already Exists')
 * 
 * 2.) showTotalEmployees - To show this do resUser.
 */
/* 
    // What will the class store? showRestaurantDetails
    IdValue = 0 
    ResUser = {
        resName : '',
        resId : 0,
        resAddress : '',
        totalGuestsSittingAsOfNow : 0,
        totalGuestsServedAsOfNow : 0,
        }  */




/* restaurantReg(restName, resaddress, resMaxCapacity, resEmployees) {
    if (resName.every(exists => resName > 0))
}       

} */




/* module.exports = ResUserClass; */


// 1.) create a function to check if the  */